{
  "name": "prize-battle",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter shared build && concurrently \"pnpm --filter shared dev\" \"pnpm --filter server dev\" \"pnpm --filter client dev\" \"pnpm --filter display dev\"",
    "build": "pnpm --filter shared build && concurrently \"pnpm --filter server build\" \"pnpm --filter client build\" \"pnpm --filter display build\"",
    "start": "pnpm --filter server start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.0"
  }
}
